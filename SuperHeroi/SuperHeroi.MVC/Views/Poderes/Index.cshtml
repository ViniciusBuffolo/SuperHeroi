@model IEnumerable<SuperHeroi.Application.ViewModels.PoderViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PoderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PoderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PoderId })
        </td>
    </tr>
}

</table>
